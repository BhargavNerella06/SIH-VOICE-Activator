# SIH Voice Activator - unified runtime requirements
# Install with: pip install -e ".[dev]"  (preferred, uses pyproject.toml)
# Or: pip install -r requirements.txt

torch>=2.0
torchaudio>=2.0
numpy
soundfile
librosa
resampy
scikit-learn
sounddevice
openwakeword
faster-whisper
fastapi
uvicorn[standard]
pydantic>=2.0
python-dotenv
# training/evaluation only (not required at runtime):
# mir_eval
# visdom
python-multipart
